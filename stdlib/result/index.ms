//! Result type, Propagate class, and combinators.

/// Result type and combinators.
export let result := import "./result";
/// Propagate type class for the `?` error-propagation operator.
export let propagate := import "./propagate";
