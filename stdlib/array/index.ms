//! Higher-order array operations.

/// Array partitioning and chunking utilities.
export let partition := import "./partition";
/// Array search utilities.
export let search := import "./search";
/// Array transformation utilities.
export let transform := import "./transform";
/// Array combination utilities.
export let combine := import "./combine";
/// Array aggregation utilities.
export let aggregate := import "./aggregate";
