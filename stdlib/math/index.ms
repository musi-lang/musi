//! Numeric utilities.

/// Math constants (PI, E, INFINITY, NAN, INT_MIN, INT_MAX).
export let constants := import "./constants";

/// Integer arithmetic operations (abs, min, max, gcd, lcm, etc.).
export let int := import "./int";

/// Floating-point operations (floor, ceil, sqrt, trig, lerp, etc.).
export let float := import "./float";
