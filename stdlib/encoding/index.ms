//! Encoding utilities.

/// Base64 encoding and decoding (RFC 4648).
export let base64 := import "./base64";

/// Hexadecimal encoding and decoding for byte arrays.
export let hex := import "./hex";
