//! Xorshift PRNG.

/// Core RNG type and basic random value operations.
export let rng := import "./rng";

/// Sampling operations (choose, shuffle, sample, gaussian).
export let sampling := import "./sampling";
