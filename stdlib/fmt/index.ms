//! Formatting utilities.

/// Integer-to-string formatters (hex, binary).
export let int_format := import "./int_format";

/// String truncation with a configurable suffix.
export let truncate := import "./truncate";
