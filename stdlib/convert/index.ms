//! Conversion utilities.

/// Into type class for value conversion.
export let into := import "./into";
/// Safe string-to-value parsing functions returning Option.
export let parse := import "./parse";
