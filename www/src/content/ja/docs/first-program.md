---
title: "最初のプログラム"
description: "小さなファイルを書き、式として読みます。"
group: "開始"
section: "開始"
order: 2
slug: "first-program"
summary: "余計な定型なしで書く最初の Musi ファイル。"
---

Musi のファイルは式として読まれます。
上から下へ読み進めます。各式は次の式が使える結果を残します。

## 最小の実行可能ファイル

{{snippet:first-file}}

`let` は名前を束縛します。`;` は式を終えます。書き始めて実行するにはこれで十分です。

## 1 つ関数を足す

{{example:double-function}}

関数も値と同じ `let` 形式で定義します。別の宣言構文へ切り替える必要はありません。

## 試す

{{try:first-program}}

## 次の一歩

上のコード例を打ち込み、`music` で実行してから [ファイル、パッケージ、エントリ](/docs/files-packages-and-entry) へ進んでください。
