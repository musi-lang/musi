# libstd (written in Musi)

Not much said here, but it should be inspired by Deno's @std/ libs on JSR, and some of Java.

for example, libstd/lang/*.ms items will be for those record types, like Str, Bool, Any, Never etc. same as how Java has java.lang.String and so on.
