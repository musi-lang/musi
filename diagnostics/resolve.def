catalog resolve music_resolve ResolveDiagKind crates/music_resolve/src/diag_catalog_gen.rs

diag ExpectedName 3200
  message "expected binding name"
  primary "expected binding name here"

diag InvalidStmt 3201
  message "invalid top-level statement"
  primary "top-level item or expression expected here"

diag UnboundName 3202
  message "unbound name `{name}`"
  primary "name `{name}` unresolved in this scope"

diag ImportResolveFailed 3203
  message "unresolved source import specifier `{spec}`"
  primary "import environment lacks specifier `{spec}`"

diag InvalidImportSpec 3204
  message "invalid import specifier"
  primary "string literal import specifier expected here"
