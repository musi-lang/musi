catalog ir music_ir IrDiagKind crates/music_ir/src/diag_catalog_gen.rs

diag InvalidSurfaceTypeId 3400
  message "invalid surface type identifier"
  primary "invalid surface type identifier"

diag LoweringRequiresSemaCleanModule 3401
  message "IR lowering requires semantic-clean module"
  primary "IR lowering requires semantic-clean module"

diag LoweringInvariantViolated 3402
  message "IR lowering invariant violated"
  primary "IR lowering invariant violated"
