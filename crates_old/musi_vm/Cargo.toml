[package]
name = "musi_vm"
description = "Stack-based virtual machine for the Musi language"
version.workspace = true
edition.workspace = true
repository.workspace = true
readme = "README.md"
license.workspace = true
keywords = ["compiler", "musi", "vm", "interpreter"]
categories = ["development-tools"]

[dependencies]
libloading = "0.9.0"
musi_codegen = { path = "../musi_codegen" }
thiserror = { workspace = true }

[dev-dependencies]
musi_lex = { path = "../musi_lex" }
musi_parse = { path = "../musi_parse" }
musi_shared = { path = "../musi_shared" }

[lints]
workspace = true
