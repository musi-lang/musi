{
	"$schema": "https://raw.githubusercontent.com/musi-lang/musi/refs/heads/main/schemas/mspackage-schema.v1.json",
	"name": "ffi-basic",
	"version": "0.1.0",
	"description": "Minimal FFI example - demonstrates extrin fn bindings without external dependencies.",
	"keywords": ["ffi", "extrin", "c"],
	"license": "MIT",
	"main": "./index.ms",
	"tasks": {
		"build-shim": {
			"description": "Compile the C shim to a shared library",
			"command": "cc -shared -fPIC -o libmusi_ffi.dylib shim.c"
		},
		"run": "DYLD_LIBRARY_PATH=. ../../target/release/musi run ./index.ms"
	}
}
