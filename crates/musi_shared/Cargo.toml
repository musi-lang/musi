[package]
name = "musi_shared"
description = "Shared infrastructure for the Musi compiler: spans, source DB, string interner, diagnostics, and typed arena"
version.workspace = true
edition.workspace = true
repository.workspace = true
readme = "README.md"
license.workspace = true
keywords = ["compiler", "musi", "infrastructure"]
categories = ["development-tools"]

[dependencies]

[lints]
workspace = true
