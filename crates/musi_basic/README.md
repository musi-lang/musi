# musi_basic

Basic utilities and shared types for Musi compiler.

## Modules

* `arena` - AST node arena allocation
* `interner` - String interning
* `source` - Source file and source map management
* `span` - Source code location tracking
* `types` - Common types like identifiers
