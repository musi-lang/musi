# musi_errors

Unified diagnostics and error handling for Musi compiler.

## Modules

* `code` - Numeric error codes (e.g. `MS1001`)
* `diagnostic` - Diagnostic details, diagnostic bag, and rendering
* `helpers` - Helper functions for creating diagnostics by archetype
* `types` - Core `MusiError` type
