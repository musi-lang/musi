---
// biome-ignore-all lint/correctness/noUnusedVariables: Astro template expressions consume frontmatter values.
interface Props {
	theme?: "warm" | "midnight" | "custom";
	scheme?: "light" | "dark";
	class?: string;
}
const {
	theme = "warm",
	scheme = "light",
	class: className = "",
	...rest
} = Astro.props;
---
<div
	{...rest}
	data-mx-root
	data-mx-theme={theme}
	data-mx-scheme={scheme}
	class:list={["mx-root", className]}
>
	<slot />
</div>
