export let concat (left : Array[Int], right : Array[Int]) : Array[Int] := [...left, ...right];

export let copy (src : Array[Int]) : Array[Int] := [...src];

export let equals (left : Array[Int], right : Array[Int]) : Bool := left = right;
