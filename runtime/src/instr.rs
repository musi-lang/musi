#[derive(Clone, Copy)]
pub enum Instr {
    Nop,
    Pop,
    Dup,
    LdC(u32),
    LdCI4(i32),
    LdCStr(u32),
    LdLoc(u32),
    StLoc(u32),
    LdArg(u32),
    Add,
    Sub,
    Mul,
    Div,
    Mod,
    Neg,
    And,
    Or,
    Xor,
    Not,
    Shl,
    Shr,
    CmpEq,
    CmpNe,
    CmpLt,
    CmpGt,
    CmpLe,
    CmpGe,
    Br(i32),
    BrTrue(i32),
    BrFalse(i32),
    Call(u32),
    CallTail(u32),
    Ret,
    NewObj(u32),
    LdFld(u32),
    StFld(u32),
    LdElem,
    StElem,
    LdLen,
    IsInst(u32),
    CastClass(u32),
    RefInc,
    RefDec,
}

pub unsafe fn decode(code: &[u8], ip: &mut usize) -> Instr {
    // TODO: decode instr from bc
    Instr::Nop
}
